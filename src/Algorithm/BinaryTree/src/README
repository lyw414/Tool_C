# BinaryTree
# 无递归
使用变长栈代替递归

# 周游
基于变长List的栈、队列实现
节点指针数据 为二叉树内部存储，存在访问风险，使用前请确保树无越界风险
节点访问函数指针 可以实现数据的安全导出，使用全局变量 + 内容拷贝方式

# 基于前序_中序/后序_中序 的二叉树的创建
对于输入遍历节点数组，使用节点地址作为 相同节点标识；该功能无实际意思，并没有基于静态存储对树进行实现
实现方法：

获取根节点 -> 解析根节点 -> 右节点存在入栈，左节点不存在出栈，右节点不存在且栈空循环结束
